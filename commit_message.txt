🧪 [testing improvement description]

🎯 What:
The `detect_type` function in `batch_process.py` lacked unit tests. This function identifies petrophysical data types (KR, MICP, RI, FF) from pandas DataFrame columns. Additionally, a bug was found where space-separated keywords (e.g. 'relative permeability') would not match properly due to whitespace replacement.

📊 Coverage:
- Validated classification for MICP, KR, RI, FF, and Unknown datasets.
- Handled edge cases including prioritization logic (MICP over KR).
- Confirmed `_col_set` normalization logic correctly handles whitespace and case mapping.
- Addressed bug by using exact matching keys aligned with normalization output (e.g. replacing 'relative permeability' with 'relative_permeability').

✨ Result:
Complete test coverage for data type classification in batch_process.py, increasing confidence during petrophysical analysis. Fixed a live detection bug for keywords containing spaces.
