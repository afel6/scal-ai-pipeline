[pytest]
norecursedirs = .venv venv env scratch hermes_skills_library test_generated
testpaths = tests
markers =
    integration: tests that talk to a live external service (e.g. the Gemini API); skipped automatically when no usable secret is available.
